/*
import { Component } from '@components/component';
import * as templateCsat from '@components/csat/csat.hbs';

export class Csat extends Component {
    /!**
     * Метод рендеринга элемента
     * @param result
     * @returns {string} html элемента оценки фильма
     *!/
    render (result) {
        return templateCsat(result);
    }
}
*/
